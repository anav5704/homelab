---
- name: update apt cache
  ansible.builtin.apt:
    update_cache: yes

- name: upgrade packages
  ansible.builtin.apt:
    upgrade: dist

- name: remove unnecessary packages
  ansible.builtin.apt:
    autoremove: true

- name: clean apt cache
  ansible.builtin.apt:
    autoclean: true
